package Capstone;

/*
Pseudocode
Class CurrencyConverterGUI:

    Constructor CurrencyConverterApplication():
        Initialize frame with title
        Set frame size
        Set default close operation to EXIT_ON_CLOSE
        Center the frame on screen
        Set frame background color to #D593FC

        Initialize and configure GUI components:
            Make Labels
            Create Dropdown lists with options

        Set up layout

        Add components to frame:
            titleLabel in NORTH position
            inputPanel in CENTER position
            resultPanel in SOUTH position

        Add ActionListener to convertButton:
            if else for error and calculations

Class CurrencyConverter:
    Declare Variables with final amounts
    Populate exchangeRates with known exchange rates
        If exchangeRates contains fromCurrency and toCurrency:
            Calculate
        Else:
            Error

 */
